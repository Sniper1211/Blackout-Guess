# 题库深度扩充计划

为了彻底解决题库告急的问题，我制定了以下扩充方案，将题库储备量提升至 **60+ 首**，足以覆盖未来两个月的“每日一题”排期。

## 1. 扩充内容：精选古诗词 50 首
我将从开源项目 `chinese-poetry` 中精选 50 首传唱度极高的唐诗宋词（避开生僻字，适合游戏），包括但不限于：
*   **唐诗**：杜甫《春望》、白居易《赋得古原草送别》、王昌龄《出塞》、王维《九月九日忆山东兄弟》等。
*   **宋词**：岳飞《满江红》、苏轼《题西林壁》、李煜《虞美人》、陆游《示儿》等。

## 2. 智能排期 SQL 脚本 (`seed_questions_v2.sql`)
我将生成一个全新的 SQL 导入脚本，具备以下特性：
*   **自动接续排期**：脚本将从 **2026-02-09**（明天）开始，自动往后顺延排期 50 天。
*   **冲突检测**：虽然无法直接读取线上数据库，但我会使用 `ON CONFLICT` 或条件插入语句，确保不会重复插入已存在的同名题目。
*   **立即发布**：所有新题目状态均设为 `published`，确保“自由模式”下立即生效。

## 3. 同步更新本地兜底 (`js/GameEngine.js`)
为了保证离线或弱网环境下的体验，我也会将这 50 首新题目同步硬编码到 `GameEngine.js` 的本地题库中。

## 4. 执行步骤
1.  创建包含 50 首诗词的 SQL 脚本。
2.  更新 `js/GameEngine.js`。
3.  提交代码。
4.  您需要在 Supabase 后台运行该脚本。

请确认执行该计划。
