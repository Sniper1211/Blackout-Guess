## 1. 界面结构调整 (index.html)
- 将 `#historyModal` 中的 `calendar-nav` 修改为包含 **年份** 和 **月份** 选择框（`<select>`）的布局，并将其置于右侧。
- 在导航栏下方添加一条分割线，匹配参考图。

## 2. 样式优化 (styles.css)
- **固定尺寸**：为 `.calendar-grid` 设置固定的行高或确保始终渲染 **6 行 (42 个格子)**，防止因月份天数不同导致弹窗高度跳动。
- **背景与配色**：
  - 设置星期表头为“一”至“日”（周一开头）。
  - 当前月份日期背景设为浅蓝色。
  - “今天”的背景色设为绿色（匹配参考图）。
  - 非本月日期文字设为灰色。
- **圆角与间距**：优化格子圆角和间距，使整体视觉更加精致、整齐。

## 3. 逻辑重构 (UIManager.js)
- **固定 42 格渲染逻辑**：
  - 修改 `renderHistoryCalendar`，计算当前展示页所需的全部 42 个日期（包含上月月末和下月月初）。
  - 调整周起始日为 **周一**。
- **下拉框联动**：
  - 初始化年、月下拉框选项。
  - 监听下拉框 `change` 事件，实时更新日历视图。
- **题目映射增强**：
  - 确保日历格子能正确关联历史题目。
  - 非本月日期如果有题目，同样支持点击跳转。

## 关键改进点
- **稳定性**：通过固定 6 行布局，彻底解决弹窗尺寸跳动问题。
- **易用性**：下拉框快速定位年月份比按钮点击更高效。
- **视觉还原**：高度还原参考图中的配色方案（浅蓝背景、绿色高亮）。

是否开始执行此优化方案？