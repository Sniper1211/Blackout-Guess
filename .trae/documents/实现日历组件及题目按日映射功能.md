## 1. 界面结构调整 (index.html)
- 在历史记录弹窗 (`#historyModal`) 中添加日历容器和导航控制（切换月份）。
- 保留现有的列表容器，作为日历模式下的详情展示或备用方案。

## 2. 样式实现 (styles.css)
- 使用 **CSS Grid** 实现 7 列布局的日历网格。
- 设计日历表头（一、二...日）、日期单元格。
- 为“有题目的日期”添加视觉标记（如小圆点或背景高亮）。
- 适配深色/浅色模式，确保日历在不同主题下都易于阅读。

## 3. 逻辑实现 (UIManager.js)
- **日历渲染核心逻辑**：
  - 实现 `renderHistoryCalendar` 方法，根据当前选择的月份生成日期格。
  - 处理日期偏移（每个月第一天是周几）。
  - 将从数据库获取的 `questions` 数组按 `publish_date` 映射到对应的日期格中。
- **交互处理**：
  - 点击日期格时，如果该日有题目，则显示题目详情或直接加载游戏。
  - 添加月份切换逻辑（上个月/下个月）。
- **“过去7天”逻辑**：确保初始打开弹窗时，默认显示当前月份，并重点标注过去 7 天的题目状态。

## 4. 数据适配 (app.js)
- 检查 `fetchPastQuestions` 逻辑，确保能获取到完整月份的数据（目前 limit 50 已足够）。
- 确保 `publish_date` 格式统一，便于在前端进行日期比对。

## 关键技术点
- 使用原生 JavaScript 处理日期对象 (`Date`)。
- 动态生成 DOM 元素并绑定事件，确保性能和交互流畅。
- 保持与现有 UI 风格（中式雅致风格）一致。

是否开始执行此方案？