# 诗词数据导入说明

## 概述

本文档说明如何将 `new-shici.json` 文件中的诗词数据导入到 Blackout-Guess 游戏的数据库中。

## 文件结构分析

### 1. 数据来源
- **文件**: `new-shici.json`
- **大小**: 3521行，包含数百首诗词
- **来源**: https://awesome-poetry.top/huajianji/www/教科书选诗.html
- **内容**: 中小学教材中的经典诗词

### 2. 数据结构
```json
{
  "source": "来源网址",
  "exclude": ["排除的分类"],
  "categories": [
    {
      "category": "分类名称",
      "url": "分类网址",
      "poems": [
        {
          "title": "诗词标题",
          "author": "作者",
          "dynasty": "朝代",
          "content": ["诗句1", "诗句2", ...],
          "url": "诗词详情网址"
        }
      ]
    }
  ]
}
```

### 3. 包含的分类
- 小学古诗（二年级上册、下册等）
- 初中古诗（七年级到九年级，课内课外）
- 高中古诗（二年级上册等）
- 总计数百首经典诗词

## 导入方案

### 方案一：使用导入脚本（推荐）

#### 准备工作
1. **安装依赖**:
   ```bash
   npm install @supabase/supabase-js
   ```

2. **配置环境变量**:
   ```bash
   # 在终端中设置
   export SUPABASE_URL="https://your-project.supabase.co"
   export SUPABASE_SERVICE_KEY="your-service-role-key"
   
   # 或者在 .env 文件中设置
   SUPABASE_URL=https://your-project.supabase.co
   SUPABASE_SERVICE_KEY=your-service-role-key
   ```

3. **获取 Supabase 密钥**:
   - 登录 Supabase 控制台
   - 进入项目设置 -> API
   - 复制 **URL** 和 **服务密钥**

#### 执行导入
```bash
# 直接导入到数据库
node import_shici.js import

# 生成 SQL 文件（手动执行）
node import_shici.js sql

# 查看帮助
node import_shici.js help
```

#### 导入配置
可以在 `import_shici.js` 中修改 `CONFIG` 对象：
- `startDate`: 起始发布日期（默认：2026-04-01）
- `daysPerCategory`: 每个分类分配的天数（默认：30）
- `batchSize`: 每批导入数量（默认：50）
- `skipExisting`: 是否跳过已存在的题目（默认：true）

### 方案二：手动执行 SQL

1. **生成 SQL 文件**:
   ```bash
   node import_shici.js sql
   ```

2. **执行 SQL**:
   - 打开 Supabase SQL 编辑器
   - 复制 `import_shici.sql` 内容
   - 执行 SQL 语句

### 方案三：管理后台批量导入

#### 功能设计
1. **文件上传界面**:
   - 支持 JSON 文件上传
   - 实时预览诗词列表

2. **导入配置**:
   - 选择导入策略
   - 设置发布日期范围
   - 配置去重选项

3. **批量导入**:
   - 显示导入进度
   - 实时反馈结果
   - 错误处理与重试

#### 实现建议
在 `admin.js` 中添加以下功能：
```javascript
// 1. 添加上传按钮和表单
// 2. 实现文件读取和解析
// 3. 添加预览表格
// 4. 实现批量导入API调用
// 5. 添加进度显示和结果反馈
```

## 数据映射关系

| JSON 字段 | 数据库字段 | 转换规则 |
|-----------|------------|----------|
| `poems.title` | `title` | 直接使用 |
| `poems.content` | `content` | 数组转字符串（用 `\n` 连接） |
| `poems.author` | `author` | 直接使用，空值转为 `''` |
| `poems.dynasty` | `dynasty` | 直接使用，空值转为 `''` |
| - | `type` | 固定为 `'poem'` |
| - | `enabled` | 固定为 `true` |
| - | `language` | 固定为 `'zh-CN'` |
| - | `status` | 固定为 `'published'` |
| - | `publish_date` | 按策略分配日期 |
| - | `published_at` | 当前时间戳 |

## 日期分配策略

### 顺序分配（默认）
- 每个分类分配连续的日期范围
- 示例：小学古诗（2026-04-01 到 2026-04-30），初中古诗（2026-05-01 到 2026-05-30）

### 随机分配
- 在指定日期范围内随机分配
- 配置：`randomDateRange.start` 和 `randomDateRange.end`

### 自定义分配
修改 `import_shici.js` 中的日期计算逻辑：
```javascript
// 自定义日期分配函数
function assignPublishDate(categoryIndex, poemIndex, totalPoems) {
  // 实现自定义逻辑
  return '2026-06-01';
}
```

## 错误处理

### 常见错误及解决方案

1. **数据库连接失败**
   - 检查 Supabase URL 和密钥
   - 验证网络连接
   - 检查数据库服务状态

2. **权限不足**
   - 使用服务密钥（service_role）
   - 检查 RLS 策略
   - 验证表权限

3. **数据格式错误**
   - 验证 JSON 文件格式
   - 检查必填字段
   - 处理特殊字符转义

4. **重复数据**
   - 启用 `skipExisting` 选项
   - 手动清理重复数据
   - 修改去重逻辑

### 错误日志
- 导入过程会生成日志文件
- 错误信息会保存到 `import_errors.log`
- 成功导入统计会显示在控制台

## 性能优化

### 批量处理
- 使用批次导入减少请求次数
- 合理设置 `batchSize`（建议 50-100）
- 添加批次间延迟避免速率限制

### 内存管理
- 流式读取大文件
- 分批处理数据
- 及时释放内存

### 并发控制
- 控制并发请求数量
- 添加请求超时
- 实现错误重试机制

## 验证与测试

### 导入前验证
1. **数据验证**:
   ```bash
   # 检查数据文件格式
   node -c new-shici.json
   
   # 统计诗词数量
   node -e "const data = require('./new-shici.json'); console.log('总诗词数:', data.categories.reduce((sum, cat) => sum + cat.poems.length, 0))"
   ```

2. **数据库验证**:
   ```sql
   -- 检查现有诗词数量
   SELECT COUNT(*) FROM question_bank WHERE type = 'poem';
   
   -- 检查日期范围
   SELECT MIN(publish_date), MAX(publish_date) FROM question_bank;
   ```

### 导入后验证
1. **数量验证**:
   ```sql
   -- 验证导入数量
   SELECT COUNT(*) FROM question_bank 
   WHERE publish_date >= '2026-04-01';
   ```

2. **数据质量**:
   ```sql
   -- 检查空值
   SELECT * FROM question_bank 
   WHERE author IS NULL OR author = '' 
   LIMIT 10;
   
   -- 检查重复
   SELECT title, author, COUNT(*) 
   FROM question_bank 
   GROUP BY title, author 
   HAVING COUNT(*) > 1;
   ```

## 维护与更新

### 定期导入
1. **增量更新**:
   - 只导入新诗词
   - 更新现有诗词信息
   - 维护版本控制

2. **计划任务**:
   ```bash
   # 每月自动导入
   0 0 1 * * cd /path/to/project && node import_shici.js import
   ```

### 数据清理
1. **清理脚本**:
   ```javascript
   // 清理无效数据
   // 更新元数据
   // 优化数据库性能
   ```

2. **备份策略**:
   - 定期备份数据库
   - 保存原始 JSON 文件
   - 维护导入日志

## 注意事项

### 安全考虑
1. **密钥管理**:
   - 不要将密钥提交到版本控制
   - 使用环境变量或配置文件
   - 定期轮换密钥

2. **数据验证**:
   - 验证输入文件来源
   - 防止注入攻击
   - 限制文件大小和类型

### 性能考虑
1. **数据库负载**:
   - 避免高峰时段导入
   - 监控数据库性能
   - 优化查询语句

2. **网络限制**:
   - 处理网络超时
   - 实现断点续传
   - 添加进度指示

### 兼容性考虑
1. **字符编码**:
   - 确保 UTF-8 编码
   - 处理特殊字符
   - 验证多语言支持

2. **时区处理**:
   - 统一使用 UTC 时间
   - 正确处理日期转换
   - 考虑用户时区

## 故障排除

### 导入失败
1. **检查日志**:
   ```bash
   # 查看错误日志
   cat import_errors.log
   
   # 查看导入日志
   cat import_shici.log
   ```

2. **测试连接**:
   ```javascript
   // 测试数据库连接
   const { data, error } = await supabase.from('question_bank').select('count');
   console.log('连接测试:', error ? error.message : '成功');
   ```

### 数据不一致
1. **验证映射**:
   ```sql
   -- 检查字段映射
   SELECT title, author, dynasty, LENGTH(content) 
   FROM question_bank 
   WHERE publish_date >= '2026-04-01' 
   LIMIT 5;
   ```

2. **修复数据**:
   ```sql
   -- 修复常见问题
   UPDATE question_bank 
   SET author = '佚名' 
   WHERE author IS NULL OR author = '';
   ```

## 支持与反馈

### 获取帮助
1. **查看文档**:
   - 本文档
   - Supabase 官方文档
   - 项目 README

2. **寻求支持**:
   - 项目 Issues
   - 社区论坛
   - 开发者文档

### 提供反馈
1. **报告问题**:
   - 描述问题现象
   - 提供错误信息
   - 附上相关日志

2. **建议改进**:
   - 功能需求
   - 性能优化
   - 用户体验

---

**最后更新**: 2026-03-23  
**版本**: 1.0.0  
**作者**: Blackout-Guess 开发团队